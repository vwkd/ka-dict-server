type Query {
  entry(id: ID!): Entry
  findEntry(value: String!): [Entry]!
}

union Entry = TargetEntry | ReferenceEntry

type TargetEntry {
  id: ID!
  source: Source!
  target: [Target!]!
}

type ReferenceEntry {
  id: ID!
  source: Source!
  reference: Reference!
}

type Source {
  value: String!
  meaning: Int!
}

type Target {
  value: [String!]!
  meaning: Int!
  tags: [Tag]!
}

type Reference {
  source: Source!
  type: Type!
  tags: [Tag]!
}

enum Type {
  DIRECT
  MEANING
  IDENTICAL
}

enum Tag {
  TUSH
  # todo: add more
}